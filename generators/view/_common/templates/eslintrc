{
    "extends": "xo",
    "env": {
        "node": false,
        "browser": true,
        "es6": <% if (props.loader === 'requirejs') { %>false<% } else { %>true<% } %>
    },<% if (props.loader === 'requirejs') { %>
    "globals": {
        "define": false,
        "require": false
    },<% } %>
    "rules": {
        "indent": [2, 4, {"SwitchCase": 1}],
        "no-invalid-this": 2
    }
}
